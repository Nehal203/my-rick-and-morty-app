import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['rickandmortyapi.com'], // allowed image domains
  },
  // additional config options here (if any)
};

export default nextConfig;
